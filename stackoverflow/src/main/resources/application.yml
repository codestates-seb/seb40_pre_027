spring:
  #Database
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/stackoverflow
    username: root
    password: ${mysql-password}
#    url: jdbc:h2:mem:test
  jpa:
    generate-ddl: true
#    database: mysql   # 추가 해준 부분
#    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect # 추가 해준 부분
    hibernate:
      ddl-auto: update
    show-sql: true  # (2) SQL ?? ??
    properties:
      hibernate:
        format_sql: true  # (3) SQL pretty print

logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG

# Security ??
jwt:
  secret-key: werewrwdfdsfsfdvvdjerthgmfghyjrgqwrjlkljklhjkghkhgjgh
  access-token-expiration-minutes: 1
  refresh-token-expiration-minutes: 360

# Test
mail:
  address:
    admin: admin@gmail.com

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
